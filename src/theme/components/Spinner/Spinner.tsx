import React from "react";

import { StyledWrapper, StyledContent } from "./Spinner.styled";

const Spinner: React.FC = () => (
  <StyledWrapper>
    <StyledContent />
  </StyledWrapper>
);

export { Spinner };
